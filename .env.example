# Supabase（脚本需用 service_role 上传 Storage 与更新表）
# 在 https://app.supabase.com 项目 → Settings → API 中复制
SUPABASE_URL=https://htrtcaswqydnfvgwernh.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI（用于生成 prompt + DALL·E 生成图片）
# 在 https://platform.openai.com/api-keys 创建
# OPENAI_API_KEY=your-openai-api-key

# 智谱AI（二选一：若不用 OpenAI 可用智谱）
# 在 https://open.bigmodel.cn 登录 → API Keys 创建
# 用于：生成 prompt（对话）+ 图像生成（CogView/GLM-Image）
# ZHIPU_API_KEY=your-zhipu-api-key

# 可选：默认风格（不传 --style 时使用）
# RECIPE_IMAGE_STYLE=中国风工笔，温暖食物摄影
